<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mursalin.onlinequizv03">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MyTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserHomeActivity" />
        <activity android:name=".AdminHomeActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".ExamDetails" />
        <!-- android:label="@string/title_activity_exam_details" /> -->
        <activity android:name=".DisplayExamListActivity" />
        <activity android:name=".QuestionPageActivity" />
        <activity android:name=".TempQuestionActivity" />
        <activity android:name=".DeleteExamActivity" />
        <activity android:name=".DisplayResListActivity"></activity>
    </application>

</manifest>